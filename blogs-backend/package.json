{
  "name": "blogs-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --test",
    "build:ui": "@powershell New-Item -ItemType Directory dist -Force && @powershell Remove-Item -Recurse -Force dist && cd ../../../fullstackcourse/blogs/ && npm run build && @powershell Copy-Item -Recurse -Force dist ../../../fullstackcourse-backend/blogs-backend/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@stylistic/eslint-plugin-js": "^2.13.0",
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.9.5"
  }
}
